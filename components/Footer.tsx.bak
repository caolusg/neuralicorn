import React from 'react';
import { useLanguage } from '../contexts/LanguageContext';

const Footer: React.FC = () => {
  const { t, language } = useLanguage();
  const year = new Date().getFullYear();

  const navItems = [
    { id: 'home', label: t.navbar.home },
    { id: 'mission', label: t.navbar.mission },
    { id: 'technology', label: t.navbar.technology },
    { id: 'progress', label: t.navbar.progress },
    { id: 'foundation', label: t.navbar.foundation },
    { id: 'contact', label: t.navbar.contact },
  ];

  const techLinks = [
    {
      label: t.technology.card1.title,
      href: `/${language}/technology/high-density-neural-interface-chip`,
    },
    {
      label: t.technology.card2.title,
      href: `/${language}/technology/mandarin-neural-decoding-model`,
    },
    {
      label: t.technology.card3.title,
      href: `/${language}/technology/implantable-bci-system-architecture`,
    },
  ];

  return (
    <footer className="relative z-10 bg-neural-black border-t border-white/5">
      <div className="max-w-7xl mx-auto px-6 py-16 grid grid-cols-1 md:grid-cols-4 gap-12">
        <div>
          <h4 className="text-white font-bold tracking-widest text-sm uppercase mb-4">{t.companyName}</h4>
          <p className="text-neutral-500 text-sm max-w-xs">{t.footer.tagline}</p>
        </div>

        <div className="md:col-span-2 grid grid-cols-2 gap-10">
          <div>
            <h5 className="text-neutral-400 text-xs uppercase mb-4">{t.footer.sitemapTitle}</h5>
            <ul className="space-y-2 text-sm text-neutral-500">
              {navItems.map((item) => (
                <li key={item.id}>
                  <a href={`/${language}/#${item.id}`} className="hover:text-white transition-colors">
                    {item.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          <div>
            <h5 className="text-neutral-400 text-xs uppercase mb-4">{t.footer.technologyTitle}</h5>
            <ul className="space-y-2 text-sm text-neutral-500">
              {techLinks.map((item) => (
                <li key={item.href}>
                  <a href={item.href} className="hover:text-white transition-colors">
                    {item.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        </div>

        <div className="md:pl-2">
          <h5 className="text-neutral-400 text-xs uppercase mb-4">{t.footer.stayUpdated}</h5>
          <p className="text-neutral-500 text-xs mb-4">{t.footer.stayUpdatedDesc}</p>
          <div className="flex items-stretch max-w-[220px]">
            <input
              type="email"
              placeholder={t.footer.subscribePlaceholder}
              className="bg-neural-dark border border-white/10 px-3 py-2 text-sm w-full focus:outline-none focus:border-white/40 text-white rounded-none"
            />
            <button className="bg-white text-black px-3 text-sm font-bold rounded-none border border-white/10 border-l-0">
              &rarr;
            </button>
          </div>
        </div>
      </div>

      <div className="max-w-7xl mx-auto px-6 py-8 border-t border-white/5 flex flex-col md:flex-row md:items-center md:justify-between gap-4 text-xs text-neutral-600">
        <span>(c) {year} {t.companyName}. {t.footer.rights}</span>
        <div className="flex gap-4">
          <span>{t.footer.privacy}</span>
          <span>{t.footer.terms}</span>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
